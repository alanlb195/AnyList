# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type List {
  id: ID!
  name: String!
  user: User!
}

type User {
  id: ID!
  fullName: String!
  email: String!
  roles: [ValidRoles!]!
  isActive: Boolean!
  updatedBy: User
  items(limit: Int! = 10, offset: Int! = 0, search: String): [Item!]!
  lists(limit: Int! = 10, offset: Int! = 0, search: String): [List!]!
  itemCount: Int!
  listsCount: Int!
}

"""Roles permitidos en el sistema"""
enum ValidRoles {
  admin
  user
  superUser
}

type Item {
  id: ID!
  name: String!
  quantityUnits: String
  user: User!
  category: String!
}

type AuthResponse {
  token: String!
  user: User!
}

type Query {
  findAllItems(limit: Int! = 10, offset: Int! = 0, search: String): [Item!]!
  findOneItem(id: ID!): Item!
  findAllUsers(roles: [ValidRoles!] = []): [User!]!
  findOneUser(id: ID!): User!
  findAllLists(limit: Int! = 10, offset: Int! = 0, search: String): [List!]!
  findOneList(id: ID!): List!
  revalidateToken: AuthResponse!
}

type Mutation {
  createItem(createItemInput: CreateItemInput!): Item!
  updateItem(updateItemInput: UpdateItemInput!): Item!
  removeItem(id: ID!): Item!
  updateUser(updateUserInput: UpdateUserInput!): User!
  blockUser(id: ID!): User!
  createList(createListInput: CreateListInput!): List!
  updateList(updateListInput: UpdateListInput!): List!
  removeList(id: ID!): List!
  signup(signupInput: SignupInput!): AuthResponse!
  login(loginInput: LoginInput!): AuthResponse!
  executeSeed: Boolean!
}

input CreateItemInput {
  name: String!
  category: String!
  quantityUnits: String
}

input UpdateItemInput {
  name: String
  category: String
  quantityUnits: String
  id: ID!
}

input UpdateUserInput {
  email: String
  fullName: String
  password: String
  id: ID!
}

input CreateListInput {
  name: String!
}

input UpdateListInput {
  name: String
  id: ID!
}

input SignupInput {
  email: String!
  fullName: String!
  password: String!
}

input LoginInput {
  email: String!
  password: String!
}